apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-b-ui
  namespace: guestbook
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-b-ui
  template:
    metadata:
      labels:
        app: guestbook-b-ui
        change: uno
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-b-ui
        ports:
        - containerPort: 80
